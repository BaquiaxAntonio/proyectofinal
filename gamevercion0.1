#include <iostream>
#include <string>

using namespace std;
int figura(int fila1,int fila2,int fila3){
for(int fila1 = 1 ; fila1<=3;fila1++){
cout<<"*";
}
cout<<"\n";
for(int fila2 = 1 ; fila2<=5;fila2++){
cout<<"*";
}
cout<<"\n";
for(int fila3 = 1 ; fila3<=7;fila3++){
cout<<"*";
}
cout<<"\n";
}



int main() {
char jugador1[50],jugador2[50];
int fila,objetos;
int fila1 = 3;
int fila2 = 5;
int fila3 = 7;
int puntosJugador1 = 0;
int puntosJugador2 = 0;

bool turnoJugador1 = true;

while (true) {
cout<<"ingrese su nombre:"<<endl;
cin>>jugador1;
cout<<"ingrese su nombre:"<<endl;
cin>>jugador2;

cout<<"!!iniciemos el juego¡¡"<<endl<<endl;

figura(0,0,0);
cout << "Puntos de " << jugador1 << ": " << puntosJugador1 << endl;
cout << "Puntos de " << jugador2 << ": " << puntosJugador2 << endl;

cout<<jugador1<<endl;
cout << "Ingrese el número de fila (1, 2 o 3): ";
cin >> fila;
cout << "Ingrese la cantidad de objetos a retirar: ";
cin >> objetos;

if (fila < 1 || fila > 3) {
cout << "Fila inválida. Por favor, seleccione una fila válida." << endl;
continue;
}
if (fila == 1 && objetos <= fila1) {
fila1 -= objetos;
} else if (fila == 2 && objetos <= fila2) {
fila2 -= objetos;
} else if (fila == 3 && objetos <= fila3) {
fila3 -= objetos;
} else {
cout << "Cantidad inválida de objetos. Por favor, seleccione una cantidad válida." << endl;
continue;
}

if (fila1 == 0 && fila2 == 0 && fila3 == 0) {
// Mostrar el nombre del jugador que ganó y actualizar puntos
if (turnoJugador1) {
cout << jugador2 << " gana!" << endl;
puntosJugador2 += 3;
} else {
cout << jugador1 << " gana!" << endl;
puntosJugador1 += 3;
}
break;
}

figura(0,0,0);
cout << "Puntos de " << jugador1 << ": " << puntosJugador1 << endl;
cout << "Puntos de " << jugador2 << ": " << puntosJugador2 << endl;
cout<<jugador2<<endl;
cout << "Ingrese el número de fila (1, 2 o 3): ";
cin >> fila;
cout << "Ingrese la cantidad de objetos a retirar: ";
cin >> objetos;

if (fila < 1 || fila > 3) {
cout << "Fila inválida. Por favor, seleccione una fila válida." << endl;
continue;
}

// Actualizar la fila y verificar que haya suficientes objetos para retirar
if (fila == 1 && objetos <= fila1) {
fila1 -= objetos;
} else if (fila == 2 && objetos <= fila2) {
fila2 -= objetos;
} else if (fila == 3 && objetos <= fila3) {
fila3 -= objetos;
} else {
cout << "Cantidad inválida de objetos. Por favor, seleccione una cantidad válida." << endl;
continue;
}

if (fila1 == 0 && fila2 == 0 && fila3 == 0) {
// Mostrar el nombre del jugador que ganó y actualizar puntos
if (turnoJugador1) {
cout << jugador2 << " gana!" << endl;
puntosJugador2 += 3;
} else {
cout << jugador1 << " gana!" << endl;
puntosJugador1 += 3;
}
break;
}

return 0;
}
}
